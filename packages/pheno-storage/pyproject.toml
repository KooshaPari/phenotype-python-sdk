[project]
name = "pheno-storage"
version = "0.1.0"
description = "PhenoSDK storage module"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pheno_storage"]

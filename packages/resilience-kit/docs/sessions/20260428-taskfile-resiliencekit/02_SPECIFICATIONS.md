# Specifications

- `build` builds the detected language workspace.
- `test` runs the detected language test suite.
- `lint` runs the detected language linter.
- `clean` removes build artifacts for the detected language.
- For this repository, Rust is the detected primary language and tasks should target `rust/`.

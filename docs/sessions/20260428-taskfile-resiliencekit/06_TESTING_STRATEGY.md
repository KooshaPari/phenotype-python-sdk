# Testing Strategy

- Run `task detect` to confirm language detection.
- Run `task build` to verify the build command.
- Run `task test` to verify the test command.
- Run `task lint` to verify the linter command.
- Run `task clean` to verify the clean command dispatch.

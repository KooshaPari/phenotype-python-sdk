# Generated CI workflow - calls reusable phenotype workflow
name: CI
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true


on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  ci:
    uses: KooshaPari/phenotype-infrakit/.github/workflows/ci.yaml@c894caa3f54e88a83791a5a5aed9ceddec552387
    secrets: inherit
